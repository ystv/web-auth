{{define "title"}}Error{{end}}
{{define "content"}}
    <section class="hero is-fullheight">
        <div class="hero-body">
            <div class="container">
                <div class="columns box">
                    <div class="column">
                        <h1>An error occurred</h1>
                        <p>We apologise for the inconvenience but the following error has occurred: <code>{{.Error}}</code><br><br>If this error does not make sense then please message Computing Team on slack.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>
{{end}}