{{define "title"}}Not found{{end}}
{{define "content"}}
    <section class="hero is-fullheight">
        <div class="hero-body">
            <div class="container">
                <div class="columns box">
                    <div class="column">
                        <h1>404 - Not found</h1>
                    </div>
                </div>
            </div>
        </div>
    </section>
{{end}}